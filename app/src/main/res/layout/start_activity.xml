<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SeekGamesFragment">

    <EditText
        android:id="@+id/name_text_edit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:imeOptions="actionSend"
        android:maxLines="1"/>

    <ImageButton
        android:id="@+id/go_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/name_text_edit"
        android:layout_alignRight="@+id/name_text_edit"
        android:layout_alignTop="@+id/name_text_edit"
        android:background="@android:drawable/ic_media_play" />


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/name_text_edit"
        android:textSize="@dimen/abc_text_size_title_material"
        android:gravity="center"
        android:text="@string/write_name" />

</RelativeLayout>
